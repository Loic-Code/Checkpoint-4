{% form_theme form 'bootstrap_4_layout.html.twig' %}
<div class="container d-flex justify-content-center">
	{{ form_start(form) }}
	{{ form_widget(form) }}
	<div class="container d-flex justify-content-center">
		<button class="btn btn-primary text-light m-2">{{ button_label|default('Save') }}</button>
	</div>
</div>
